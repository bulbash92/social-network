import React from 'react';
import LoginForm from "./LoginForm";

const Login = () => {
    return (
        <div style={{marginLeft: '20px'}}>
          <h1>Login</h1>
           <LoginForm/>
        </div>
    );
};

export default Login;